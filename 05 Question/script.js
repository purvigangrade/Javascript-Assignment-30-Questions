//Q5. Declare a varibale and assign string value to it and then split it into an array using split() method

let splitMethod = 'The split() method splits a string into an array of substrings.';

let splitArray = splitMethod.split(' ');
console.log(splitArray);